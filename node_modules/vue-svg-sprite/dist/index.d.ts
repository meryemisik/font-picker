import _Vue, { VNode, VNodeDirective } from 'vue';
interface ISvgSpriteOptions {
    class?: string;
    url?: string;
}
declare const _default: {
    options: ISvgSpriteOptions;
    ssr(vnode: VNode, directiveMeta: VNodeDirective): void;
    install(vue: import("vue").VueConstructor<_Vue>, opts?: ISvgSpriteOptions): void;
    getAttributes(size: string | number): {
        height: string;
        viewBox: string;
        width: string;
    };
    hasClass(classes: string): boolean;
    getHref(id: string): string;
};
export default _default;
